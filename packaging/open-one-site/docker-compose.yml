services:
  open-one-site:
    image: nginx:1.27-alpine
    restart: unless-stopped
    ports:
      - "${OPEN_ONE_SITE_PORT:-13006}:80"
    volumes:
      - ./site:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
